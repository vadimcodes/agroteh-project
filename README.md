# agroteh-project
> Сайт Агротех21
```
├── cd weblayout                # Перейти в директорию
├── git status                  # Проверить статус
├── git checkout -b dev         # создать вертку, где `dev` наименование ветки
├── git pull origin master      # Загрузить объекты в GitHub
├── git checkout dev            # Перейти в ветку,где `dev` наименование ветки
├── git add .                   # Добавить объекты, где `.` добавить все файлы
├── git commit -m "add header"  # Коммиты (коментарии) к заданию, где `"add header"` коммит
├── git push origin dev         # Запушить задание в GitHub

```
